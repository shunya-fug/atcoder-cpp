#include <bits/stdc++.h>
#define REP(i, n) for (int i = 0; (i) < (int)(n); ++ (i))
#define REP3(i, m, n) for (int i = (m); (i) < (int)(n); ++ (i))
#define REP_R(i, n) for (int i = (int)(n) - 1; (i) >= 0; -- (i))
#define REP3R(i, m, n) for (int i = (int)(n) - 1; (i) >= (int)(m); -- (i))
#define ALL(x) ::std::begin(x), ::std::end(x)
using namespace std;

const std::string YES = "Yes";
const std::string NO = "No";
auto solve(int64_t N, int M, int Q, const std::vector<int64_t> &X, const std::vector<int64_t> &Y, const std::vector<int64_t> &A, const std::vector<int64_t> &B) {
    // TODO: edit here
}

// generated by oj-template v4.8.1 (https://github.com/online-judge-tools/template-generator)
int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    int64_t N;
    int M, Q;
    std::cin >> N >> M;
    std::vector<int64_t> X(M), Y(M);
    std::cin >> Q;
    std::vector<int64_t> A(Q), B(Q);
    REP (i, M) {
        std::cin >> X[i] >> Y[i];
    }
    REP (i, Q) {
        std::cin >> A[i] >> B[i];
    }
    auto ans = solve(N, M, Q, X, Y, A, B);
    REP (i, Q) {
        std::cout << a[i] << '\n';
    }
    return 0;
}
